const { $ } = require('zx');
const { step } = require('./utils');

function yarnInstallVue2() {
  // step('\n yarn install dev/app-vue ...');
  // $`cd ./dev/app-vue && rm -rf node_modules/ && yarn install`;
  // step('\n yarn install dev/app-vue-2 ...');
  // $`cd ./dev/app-vue-2 && rm -rf node_modules/ && yarn install`;
}

yarnInstallVue2();
